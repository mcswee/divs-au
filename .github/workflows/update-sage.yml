name: Update SAGE Index

on:
  schedule:
    # Monday 6 AM AEST (Sunday 20:00 UTC) — picks up weekend results
    - cron: "0 20 * * 0"
    # Thursday 6 AM AEST (Wednesday 20:00 UTC) — picks up mid-week games
    - cron: "0 20 * * 3"
  workflow_dispatch:  # allow manual trigger

permissions:
  contents: write

jobs:
  update-sage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run SAGE computation
        run: python scripts/compute_sage.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/data/sage.json
          git diff --cached --quiet || git commit -m "chore: update SAGE index [$(date -u '+%Y-%m-%d')]"
          git push
